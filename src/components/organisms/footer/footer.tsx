import React from 'react';
import { compContainer } from './footer.module.scss';

export default function Footer() {

    return (
        <>
            <footer className={compContainer}>
                <p>This is the footer</p>
            </footer>
        </>
    )
}